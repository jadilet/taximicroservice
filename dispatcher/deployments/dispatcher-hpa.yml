apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: dispatcher-srv
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: dispatcher-srv
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 50